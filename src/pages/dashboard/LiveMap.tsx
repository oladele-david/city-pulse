import { LiveMapComponent } from "../../components/dashboard/map/LiveMapComponent";

const LiveMap = () => {
    return (
        <div className="w-full h-full absolute inset-0">
            <LiveMapComponent />
        </div>
    );
};

export default LiveMap;
